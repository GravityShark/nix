#!/bin/sh

# made by shit gippity

if [ "$#" -ne 1 ]; then
  echo "Usage: $0 <command>"
  exit 1
fi

COMMAND="$*"

/bin/sh -c "$COMMAND"

while :; do
  echo "Press Enter to repeat: $COMMAND"
  read a
  eval "$COMMAND"
done
